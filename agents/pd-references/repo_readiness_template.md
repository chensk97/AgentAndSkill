# Repository Readiness Check

推荐存放路径：`<project_root>/Agent_doc/Other/Repo_Readiness_Check.md`

## 1. 检查范围

- 项目根目录：
- Git 远端：
- 目标主干：`main`
- 检查时间：
- 检查角色 / 技能：

## 2. 远端与鉴权

- 远端地址：
- `git fetch` 结果：
- 鉴权状态：正常 / 异常
- 阻塞说明：

## 3. 基线分支状态

- 当前分支：
- `main` 是否存在：是 / 否
- `origin/main` 是否可访问：是 / 否
- 当前工作树是否 clean：是 / 否
- 最近提交：

## 4. Agent_doc 可归档性

- `Agent_doc/` 是否存在：是 / 否
- 是否被 ignore 规则屏蔽：是 / 否
- 必要子目录是否齐备：是 / 否
- 风险说明：

## 5. 结论

- 综合结论：可继续 / 需先修复
- 关键阻塞：
- 建议下一步：

## 6. 检查清单

- [ ] 远端存在且可访问
- [ ] `git fetch` 成功
- [ ] `main` 与 `origin/main` 可见
- [ ] 当前工作树状态满足后续分支操作
- [ ] `Agent_doc/` 未被 ignore 规则屏蔽
- [ ] 已记录阻塞与建议恢复动作